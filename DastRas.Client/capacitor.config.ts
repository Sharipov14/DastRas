import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dastras.client',
  appName: 'DastRas',
  webDir: 'dist/dast-ras.client/browser'
};

export default config;
